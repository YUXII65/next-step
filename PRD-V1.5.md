# 下一步 V1.5 产品需求文档

> 版本：0.1  
> 日期：2026-08-08  
> 状态：需求草案  
> 定位：V1.5 升级 AI 智慧水平的设计基线，约束后续开发、数据模型和验收标准

## 1. 产品判断

V1 已经完成了「输入 -> 目标 -> 行动 -> 复盘」的基础闭环。V1 的 AI 仍然是单次生成能力：每次请求拿到当前输入和少量统计信息，输出一个计划或建议。

V1.5 的核心目标不是让模型“更聪明”，而是让 AI 真正形成持续理解用户的能力：

- 知道用户长期目标和项目当前状态
- 知道用户过去做过什么、没做什么、为什么调整
- 知道用户对 AI 建议的接受、修改、忽略和延迟模式
- 每个建议都有真实依据，而不是泛泛的“建议优先完成”
- 每次用户操作都成为下一次 AI 建议的上下文

一句话定位：

> V1.5 把 AI 从“一次性整理工具”升级为“越来越懂这个用户的规划教练”。

## 2. 用户价值

V1.5 应该让用户明确感受到三个变化：

1. AI 不再只基于当前一句话做计划，而是会引用项目进度、最近复盘和用户历史偏好。
2. AI 给每个建议都能说清楚依据，例如“项目 X 已停 6 天”“这个任务已推迟 2 次”。
3. 用户修改、忽略、完成或推迟任务后，AI 下次建议会自然调整，不再重复给出同一类不合适的计划。

## 3. V1.5 目标

### 3.1 目标

- 建立可复用的用户记忆和项目记忆。
- 建立完整的 AI 反馈闭环。
- 让核心 AI 入口都使用统一上下文协议。
- 让 AI 输出具备可解释性。
- 保持 V1 的降级能力，AI 不可用时仍可手动完成闭环。
- 建立可评估的 AI 质量基线。

### 3.2 非目标

V1.5 不做以下内容：

- 不做复杂 RAG、向量知识库和全文检索
- 不做 AI Agent 自动执行
- 不做多模型协作
- 不做多用户、权限系统
- 不做浏览器插件、移动端、日历同步
- 不做复杂统计报表
- 不做自动通知和频繁打扰

## 4. 核心概念

### 4.1 用户记忆

用户记忆是 AI 对用户长期偏好和推进模式的结构化描述，包括：

- 用户明确表达的偏好
- 从行为推断出的偏好
- 最近复盘内容
- 近期接受、忽略、修改 AI 建议的模式
- 近期任务完成、取消、推迟情况

### 4.2 项目记忆

项目记忆是 AI 判断项目是否健康推进的依据，包括：

- 项目名称、目标、当前里程碑
- 最近推进时间
- 连续未推进天数
- 项目下任务数量和状态
- 项目最近一次复盘状态

### 4.3 任务记忆

任务记忆用于任务卡住对话和今日建议，包括：

- 任务创建来源
- 创建时间、计划日期、截止日期
- 状态变化历史
- 被推迟的次数
- 最近一次完成或取消的原因上下文

### 4.4 反馈事件

反馈事件是 AI 学习的核心数据，记录用户对 AI 建议的后续行为：

- 接受 AI 计划
- 忽略 AI 建议
- 修改 AI 建议字段
- 推迟 AI 计划任务
- 完成 AI 计划任务
- 取消 AI 计划任务
- 对 AI 建议给出正面或负面反馈

### 4.5 AI 上下文协议

所有 AI 调用不再直接拼接一段 summary，而是通过统一 Context Builder 生成结构化上下文，再转换为 prompt 输入。

上下文至少包含：

- 当前输入
- 相关项目
- 最近复盘
- 任务/项目历史
- 用户偏好
- 近期反馈摘要
- 今天日期和相对时间

## 5. 范围拆分

### 5.1 Phase 1：记忆与反馈基础设施

Phase 1 是 V1.5 的地基，先不升级界面，优先补齐数据和上下文能力。

范围：

- 新增用户偏好数据结构
- 新增通用 AI 反馈事件数据结构
- 建立项目停滞时间计算
- 建立统一 Context Builder
- 把收件箱计划的接受、忽略、编辑数据完整记录下来
- 把任务状态变化记录为反馈事件
- 为今日重点建议增加可解释依据

验收标准：

- AI 上下文可以回答“这个项目多久没推进”
- AI 上下文可以回答“这个任务是否来自 AI 计划”
- AI 上下文可以回答“用户近期修改过哪些计划字段”
- 用户忽略或修改 AI 建议后，下一次调用能看到对应反馈
- 核心 AI 入口不再只依赖旧的 600 字 summary

### 5.2 Phase 2：AI 入口个性化

范围：

- AI 任务整理：使用项目记忆、最近复盘和用户偏好
- 今日重点：使用项目停滞、任务延迟历史和复盘结论
- 每日复盘：参考最近 3-7 天趋势，而不只参考当天
- 任务卡住对话：使用任务历史和用户近期反馈
- AI 修改建议：记录最终修改结果，形成偏好推断

验收标准：

- 每个 AI 入口都使用统一 Context Builder
- 每个建议都能返回至少一条真实依据
- 用户连续修改同类字段时，AI 会在建议中主动调整

### 5.3 Phase 3：评测与调优

范围：

- 建立固定 AI 测试语料
- 定义人工评分标准
- 记录线上建议接受率、编辑率、任务完成率、延期率
- 支持 prompt 版本化对比
- 支持在 DeepSeek 和其他 OpenAI 兼容模型之间切换验证

验收标准：

- 有至少 10 条固定输入作为回归语料
- 每次修改 AI 策略后可以人工评分
- 核心指标可以在本地查看或导出

## 6. 数据模型设计

### 6.1 用户偏好

```prisma
model UserPreference {
  id        String   @id @default(cuid())
  key       String
  value     String
  source    String   @default("manual")
  note      String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([key, source])
  @@map("user_preferences")
}
```

建议优先支持的 key：

- `plan_scale`：用户偏好的计划规模
- `default_start_action`：更倾向最小动作还是完整方案
- `avoid_overdue`：是否更看重按时完成
- `project_focus`：当前最需要推进的项目

### 6.2 通用 AI 反馈事件

```prisma
model AiFeedbackEvent {
  id           String   @id @default(cuid())
  source       String
  action       String
  inboxItemId  String?
  taskId       String?
  projectId    String?
  beforeJson   String?
  afterJson    String?
  detail       String?
  createdAt    DateTime @default(now())

  @@map("ai_feedback_events")
}
```

action 建议枚举：

- `plan_accepted`
- `plan_ignored`
- `plan_edited`
- `task_completed`
- `task_delayed`
- `task_cancelled`
- `suggestion_feedback`

### 6.3 项目停滞时间

不单独建表，通过任务更新时间、项目 updatedAt、最近复盘时间计算。

计算规则：

- 项目最后推进时间 = max(最近任务 updatedAt, 项目 updatedAt, 最近相关复盘时间)
- 停滞天数 = 今天 - 最后推进时间
- 如果项目没有任务且从未复盘，不强行提示停滞

## 7. AI Context Builder 设计

建议新增 `src/lib/ai-context.ts`，统一提供：

```ts
type AiContextInput = {
  kind: "inbox_plan" | "today_focus" | "review" | "task_coach" | "task_edit" | "project_edit";
  inboxItemId?: string;
  taskId?: string;
  projectId?: string;
  content?: string;
};

type AiContext = {
  userPreferenceSummary: string;
  projectSummary: string;
  taskHistorySummary: string;
  reviewSummary: string;
  feedbackSummary: string;
  evidence: string[];
};
```

实现原则：

- 不把所有数据都塞进 prompt
- 每个入口只选择相关数据
- 上下文限制在 1500 字以内
- 证据必须来自真实数据
- 上下文生成失败时降级为当前 summary

## 8. 用户反馈采集规则

### 8.1 收件箱计划

- 用户确认计划：记录 `plan_accepted`
- 用户忽略计划：记录 `plan_ignored`
- 用户编辑计划后确认：记录 `plan_edited`，保存 before/after JSON

### 8.2 任务状态

- 任务完成：记录 `task_completed`
- 任务取消：记录 `task_cancelled`
- 任务截止日期被延后：记录 `task_delayed`

### 8.3 建议质量

今日建议和任务卡住对话可以增加轻量反馈入口：

- 有用
- 没用
- 原因可选

## 9. AI 输出规范升级

### 9.1 建议必须带依据

今日重点示例：

```json
{
  "title": "完成作品集第一版",
  "reason": "项目已停 6 天，且这是最近复盘里唯一明确的下一步。"
}
```

禁止输出：

```text
建议优先完成重要任务。
```

### 9.2 计划必须控制规模

AI 计划任务数量上限保持不变，但 Context Builder 会增加用户偏好提示：

- 如果用户近期多次编辑任务数量，计划数量应更保守
- 如果项目已经停滞，首条任务应是 10 分钟可启动动作
- 如果用户连续推迟，应提醒用户降低承诺，而不是继续增加任务

## 10. 指标定义

### 10.1 核心指标

| 指标 | 定义 | 目标 |
| --- | --- | --- |
| AI 计划接受率 | 接受计划数 / 展示计划数 | 先记录，不设硬指标 |
| AI 计划编辑率 | 编辑后确认数 / 接受数 | 先记录，用于判断个性化 |
| 计划任务完成率 | 来自 AI 的任务中已完成的占比 | 持续观察 |
| 任务延期率 | 有截止日期但延后的任务占比 | 持续观察 |
| 今日重点执行率 | 今日重点中当天完成的比例 | 持续观察 |
| 复盘连续率 | 连续完成复盘的天数 | 持续观察 |

### 10.2 评测语料评分标准

每条测试输入按 5 分制评分：

- 是否理解用户真实目标
- 计划是否可执行
- 是否过度规划
- 是否引用真实项目上下文
- 建议是否有具体依据

## 11. V1.5 验收路径

1. 用户连续使用 7 天，核心闭环不回归
2. 用户可以在界面或数据中看到 AI 建议依据
3. 用户修改、忽略、推迟后，AI 下一次建议产生可观察变化
4. AI 不可用时，手动闭环仍可完整完成
5. 测试语料中至少 80% 的计划可执行且不脱离上下文

## 12. 下一步执行建议

1. 先落地 Phase 1 数据模型和 Context Builder
2. 再升级收件箱计划和今日建议
3. 然后升级复盘和任务卡住对话
4. 最后建立评测语料，持续调优 prompt
